"""Reading discovery (Phase 4 step 2): find work the corpus has no edge to.

`arxiv` harvests public metadata (category + date only — nothing about him leaves), `profiles`
builds the vectors his work is represented by, and `rank` scores candidates against them locally.
"""
